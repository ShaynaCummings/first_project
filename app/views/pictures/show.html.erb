<div class="container">

  <p>
  <img src="<%= @picture.url %>">
  </p>

  <p>
  <strong>Added by:</strong> <%= link_to @picture.user.name, user_path(@picture.user) %>
  </p>

  <p>
  <strong>Tags:</strong> <% @picture.tags.each do |tag| %>
  #<%= tag.tag_name %>
  <% end %>
  </p>

  <%= form_for @tag do |f|  %>
  <p>
    <%= f.label "add a tag:" %>
    <%= f.text_field :tag_name %>

    <%= f.hidden_field :picture_id %>
    <%= f.hidden_field :user_id %>

    <%= f.submit "add!" %>
  </p>

  <br/>
  <p>
    <strong>Original gif link:</strong> <%= link_to @picture.url, @picture.url %>
  </p>

  <p>
    <strong>Gfycat link:</strong> <%= link_to @picture.gfy_url, @picture.gfy_url %>
  </p>

  <br/>

  <p>
    <%= button_to "Remove this gif", @picture, method: :delete if self.current_user == @picture.user %>
    <% end %>
  </p>

  <p>
    <%= link_to "back to library", pictures_path %>
  </p>

</div>
